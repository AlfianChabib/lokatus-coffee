export default function Header() {
  return (
    <nav className="fixed top-0 flex w-full justify-center">
      <div className="flex w-full max-w-md items-center justify-between px-2 py-3">
        <h1 className="text-xl font-semibold">Quote.</h1>
      </div>
    </nav>
  );
}
